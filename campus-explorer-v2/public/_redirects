# ============================================================
# Netlify 重定向规则
# Vite 构建时自动复制到 dist/_redirects
# Netlify 部署时读取此文件
# ============================================================

# AI API 代理 — /api/ai/* 转发到阿里云 DashScope
/api/ai/*  https://dashscope.aliyuncs.com/:splat  200!

# SPA 回退 — 所有非文件路径返回 index.html（Vite 项目必需）
/*         /index.html                             200
